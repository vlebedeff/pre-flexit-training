import * as React from "react";

interface IToolboxComponentProps {
  
}

export class ToolboxComponent extends React.Component<IToolboxComponentProps, {}> {
  render() {
    return (
      <div className="c-toolbox">
        
      </div>
    );
  }
}